name: daily-scan

on:
  schedule:
    - cron: '30 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm install

      - name: Run scanner
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
          CHANNELS: ${{ vars.CHANNELS }}
          TEST_LATEST: ${{ vars.TEST_LATEST }}
          TEST_VIDEO_ID: ${{ vars.TEST_VIDEO_ID }}
        run: node yt-follower.js

      - name: Save seen state
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -f seen-videos.json ]; then
            git add seen-videos.json
            git diff --cached --quiet || { git commit -m "update seen-videos state"; git push; }
          else
            echo "state file not created yet, nothing to commit"
          fi